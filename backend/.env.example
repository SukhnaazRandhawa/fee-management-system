# PostgreSQL Database Configuration
JWT_SECRET=
DATABASE_URL=
DB_USER=
DB_HOST=
DB_NAME=
DB_PASSWORD=
PORT=5050
RESEND_API_KEY=
# Sandbox domain only delivers to your own Resend account email; switch to a
# verified custom domain address before onboarding real users.
EMAIL_FROM=onboarding@resend.dev
# Local dev: http://localhost:3000
# Production: your deployed frontend URL (e.g. Vercel domain)
FRONTEND_URL=http://localhost:3000
